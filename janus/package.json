{
  "name": "@evoclock/janus",
  "version": "0.1.0",
  "private": true,
  "description": "janus \u2014 Jev evaluation wrapper service. The only AI SDK import site in pi-agentic-driver.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx janus/server.ts",
    "start": "tsx janus/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx tests/*.test.ts"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0",
    "@evoclock/redaction": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "24.10.9",
    "tsx": "4.21.0",
    "typescript": "5.9.2"
  },
  "allowScripts": {
    "esbuild@0.27.7": true
  }
}
